<links-menu class="{ (menus && menus.length > 0) ? '' : 'hide' }">
    <style>
        :scope {
            margin: 0 auto;
        }
    </style>
    <script>
        //#region riot handlers

        this.on('mount', () => { });
        this.on('unmount', () => { });

        //#endregion
    </script>
</links-menu>